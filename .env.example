# Copie para .env e ajuste. .env é ignorado pelo git.

# API do ecommerce-synthetic-data (../ecommerce-synthetic-data), rodando nativo via `npm start` (ver
# stack.sh).
ECOMM_DATA_API_URL=http://localhost:3000

# Piso de data pra replicação incremental da stream orders (única com campo
# de data por registro, creationDate). Usado só na primeira run incremental
# (a partir daí o bookmark de state assume). Formato ISO 8601.
ECOMM_ORDERS_START_DATE=2020-01-01T00:00:00Z

# Export do GA4 lido dos arquivos locais por scripts/load_ga4_customer_behavior.py.
# Caminho relativo à raiz do ecommerce-data-pipeline (stack.sh sempre roda os scripts a
# partir daqui).
GA4_SOURCE_GLOB="../ga4_bigquery_export/events/events_*.json.gz"
# Opcional: limita quantos dias processar (vazio = todos).
MAX_DAYS=

# Warehouse ClickHouse nativo (ver stack.sh — sobe via ~/.local/bin/clickhouse).
# CLICKHOUSE_PASSWORD não pode ficar vazio: o driver HTTP usado pelo
# target-clickhouse e pelo dbt-clickhouse manda Basic Auth com a senha, e o
# ClickHouse rejeita Basic Auth com senha vazia (mesmo pro usuário "default"
# sem senha configurada) — defina qualquer valor não-vazio.
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DATABASE=raw
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=clickhouse_local_dev

# Serving store do ml-api: variável usada pelo projeto irmão ../ecommerce-machine-learning
# (ver ecommerce-machine-learning/.env.example), não por este projeto.

# Backup/restore de metabase/data (conexões e dashboards salvos) via
# `./stack.sh backup-dashboard` / `restore-dashboard`, usando um bucket
# Cloudflare R2 e o CLI nativo `wrangler` (npm install -g wrangler).
# CLOUDFLARE_ACCOUNT_ID: painel da Cloudflare > canto direito de qualquer
#   página do dashboard (ou R2 > Overview).
# CLOUDFLARE_API_TOKEN: My Profile > API Tokens > Create Token > template
#   "Edit Cloudflare Workers" ou um token custom com permissão
#   "Workers R2 Storage: Edit".
# R2_BUCKET: nome do bucket (crie um dedicado, ex. "ecommerce-data-pipeline-backups").
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
R2_BUCKET=
